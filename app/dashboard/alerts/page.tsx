import type { Metadata } from "next";
import Link from "next/link";
import { StartMembershipButton } from "@/components/membership-actions";
import { DashboardPageShell } from "@/components/dashboard-page-shell";
import { SignalBeam } from "@/components/signal-beam";
import { getCurrentSnapshot } from "@/lib/auth";
import { getLatestNetworkMetricSnapshot, type NetworkSignal, type SignalLifecycle } from "@/lib/dashboard-storage";
import { hasPremiumAccess, membershipLabel } from "@/lib/membership";
import { serverNowSeconds } from "@/lib/server-time";

export const metadata:Metadata={title:"Alerts | FateDrop Dashboard",description:"Live FateDrop lifecycle signals for collectors.",robots:{index:false,follow:false}};
const stateMeta:Record<SignalLifecycle,{label:string;glyph:string;description:string}>={whisper:{label:"WHISPER",glyph:"W",description:"Early catalogue activity before verified availability."},manifested:{label:"MANIFESTED",glyph:"M",description:"Verified purchasable stock detected."},vanished:{label:"VANISHED",glyph:"V",description:"Previously purchasable stock is no longer verified available."},echo:{label:"ECHO",glyph:"E",description:"Previously available stock has returned."}};
function money(pence:number|null|undefined){return pence==null?"—":new Intl.NumberFormat("en-GB",{style:"currency",currency:"GBP"}).format(pence/100)}
function relativeTime(timestamp:number,now:number){const seconds=Math.max(0,now-timestamp);if(seconds<60)return"just now";if(seconds<3600)return`${Math.floor(seconds/60)}m ago`;if(seconds<86400)return`${Math.floor(seconds/3600)}h ago`;return`${Math.floor(seconds/86400)}d ago`}
function SignalCard({signal,unlocked,now}:{signal:NetworkSignal;unlocked:boolean;now:number}){const meta=stateMeta[signal.state],title=unlocked?signal.title:"Premium signal detail",retailer=unlocked?(signal.retailer||"Retailer pending"):"Retailer hidden",detail=unlocked?(signal.detail||meta.description):"Unlock Premium to reveal product, retailer and actionable signal context.";return <article className={`fd-signal-card state-${signal.state} ${unlocked?"":"locked"}`}><div className="fd-signal-card-top"><span><i>{meta.glyph}</i>{meta.label}</span><time>{relativeTime(signal.occurredAt,now)}</time></div><div className={unlocked?"":"fd-alert-blur"}><h2>{title}</h2><p>{retailer}</p></div><SignalBeam pulseKey={signal.id} state={signal.state}/><div className={`fd-signal-detail ${unlocked?"":"fd-alert-blur"}`}>{detail}</div><footer><span><small>TRUE PRICE</small><b>{unlocked?money(signal.deliveredPricePence):"£—.——"}</b></span><span><small>DETECTED</small><b>{relativeTime(signal.occurredAt,now)}</b></span><span><small>STATE</small><b>{meta.label}</b></span></footer>{!unlocked?<div className="fd-alert-lock">♛</div>:null}</article>}
export default async function AlertsPage(){const snapshot=await getCurrentSnapshot(),network=await getLatestNetworkMetricSnapshot(),unlocked=snapshot?hasPremiumAccess(snapshot.membership):false,plan=snapshot?membershipLabel(snapshot.membership):"Free",now=serverNowSeconds(),signals=[...(network?.recentSignals??[])].sort((a,b)=>b.occurredAt-a.occurredAt),trialEligible=Boolean(snapshot&&!snapshot.membership.stripeCustomerId&&!snapshot.membership.trialStartedAt),hasOpenSubscription=Boolean(snapshot?.membership.stripeSubscriptionId&&snapshot.membership.status!=="canceled");const counts={whisper:signals.filter(i=>i.state==="whisper").length,manifested:signals.filter(i=>i.state==="manifested").length,vanished:signals.filter(i=>i.state==="vanished").length,echo:signals.filter(i=>i.state==="echo").length};return <DashboardPageShell title="Alerts" eyebrow="LIVE SIGNAL NETWORK"><div className="fd-alerts-content"><section className="fd-alert-hero"><div><span>FATEDROP // SIGNAL ENGINE</span><h1>When the network moves,<br/><em>you should feel it.</em></h1><p>Every real lifecycle event arrives as a FateDrop signal. A beam marks the moment intelligence resolves into something worth your attention.</p></div><div className="fd-alert-orbit" aria-hidden="true"><i/><i/><b>◇</b></div><div className="fd-alert-counts"><span><b>{counts.whisper}</b>WHISPER</span><span><b>{counts.manifested}</b>MANIFESTED</span><span><b>{counts.echo}</b>ECHO</span><span><b>{counts.vanished}</b>VANISHED</span></div></section>{!unlocked?<section className="fd-alerts-gate"><div><span>PREMIUM SIGNAL INTELLIGENCE</span><h2>The signal is visible. The actionable intelligence is locked.</h2><p>{plan} members can see network movement. Plus and Pro reveal product, retailer and price context.</p></div>{hasOpenSubscription?<Link className="button button-primary" href="/dashboard/membership">Manage membership →</Link>:<StartMembershipButton tier="plus" label={trialEligible?"Start free trial":snapshot?.membership.stripeCustomerId?"Restart Plus":"Choose Plus"}/>}</section>:null}<section className="fd-alerts-feed"><div className="fd-alerts-feedhead"><div><span>LIVE SIGNAL CARDS</span><small>{network?`Source: ${network.source}`:"Awaiting FateDrop Cloud"}</small></div><b>{signals.length} SIGNAL{signals.length===1?"":"S"}</b></div>{signals.length?<div className="fd-signal-grid">{signals.map(signal=><SignalCard key={signal.id} signal={signal} unlocked={unlocked} now={now}/>)}</div>:<div className="fd-alerts-empty"><span>◇</span><h2>The network is quiet.</h2><p>When FateDrop Cloud publishes a lifecycle event, its signal card will materialise here automatically.</p></div>}</section></div><style>{`.fd-alerts-content{display:grid;gap:18px}.fd-alert-hero{position:relative;overflow:hidden;min-height:300px;padding:34px;border:1px solid rgba(157,109,255,.18);border-radius:24px;background:radial-gradient(circle at 78% 50%,rgba(88,232,255,.12),transparent 23%),radial-gradient(circle at 92% 15%,rgba(157,72,255,.16),transparent 28%),linear-gradient(140deg,#0b0913,#08080d)}.fd-alert-hero>div:first-child{position:relative;z-index:2;max-width:690px}.fd-alert-hero span{color:#73e9fb;font-size:9px;font-weight:900;letter-spacing:.18em}.fd-alert-hero h1{margin:13px 0;font-size:clamp(2.4rem,4.5vw,4.6rem);line-height:.91;letter-spacing:-.055em}.fd-alert-hero h1 em{font-style:normal;background:linear-gradient(90deg,#fff,#a5efff,#bd94ff);-webkit-background-clip:text;color:transparent}.fd-alert-hero p{max-width:600px;color:#9c95a4;font-size:14px;line-height:1.65}.fd-alert-orbit{position:absolute;right:8%;top:50%;width:190px;height:190px;transform:translateY(-50%)}.fd-alert-orbit i{position:absolute;inset:0;border:1px solid rgba(119,228,255,.13);border-radius:50%}.fd-alert-orbit i:nth-child(2){inset:28px;border-color:rgba(177,104,255,.18)}.fd-alert-orbit b{position:absolute;left:50%;top:50%;transform:translate(-50%,-50%) rotate(12deg);width:60px;height:84px;display:grid;place-items:center;border:1px solid rgba(157,109,255,.38);border-radius:8px;color:#aeefff;background:rgba(157,109,255,.08);box-shadow:0 0 55px rgba(111,93,255,.2)}.fd-alert-counts{position:absolute;left:34px;bottom:25px;display:flex;gap:28px}.fd-alert-counts span{color:#6d6774;font-size:7px}.fd-alert-counts b{display:block;color:#fff;font-size:17px;letter-spacing:0}.fd-alerts-gate{border:1px solid rgba(155,92,255,.3);background:linear-gradient(100deg,rgba(79,30,141,.17),rgba(10,9,15,.95));border-radius:18px;padding:22px;display:flex;align-items:center;justify-content:space-between;gap:24px}.fd-alerts-gate span{font-size:9px;letter-spacing:.16em;color:#ad77ff;font-weight:800}.fd-alerts-gate h2{font-size:19px;margin:6px 0}.fd-alerts-gate p{color:#918a99;margin:0;font-size:12px}.fd-alerts-feed{border:1px solid rgba(255,255,255,.075);background:#09080d;border-radius:22px;overflow:hidden}.fd-alerts-feedhead{display:flex;justify-content:space-between;padding:18px 22px;border-bottom:1px solid #1b1820}.fd-alerts-feedhead span{display:block;font-size:10px;letter-spacing:.14em;font-weight:850}.fd-alerts-feedhead small,.fd-alerts-feedhead b{color:#716b79;font-size:9px}.fd-signal-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:1px;background:#19161e}.fd-signal-card{position:relative;padding:20px;background:radial-gradient(circle at 100% 0%,rgba(157,109,255,.055),transparent 30%),#0b0a10}.fd-signal-card-top{display:flex;justify-content:space-between;align-items:center}.fd-signal-card-top>span{display:flex;align-items:center;gap:8px;color:#8d8695;font-size:8px;font-weight:900;letter-spacing:.13em}.fd-signal-card-top i{width:26px;height:26px;display:grid;place-items:center;border:1px solid rgba(157,109,255,.2);border-radius:8px;color:#b794ff;font-style:normal}.fd-signal-card-top time{color:#655f6b;font-size:9px}.fd-signal-card h2{margin:16px 0 3px;font-size:17px}.fd-signal-card p{margin:0 0 13px;color:#817a88;font-size:10px}.fd-signal-detail{min-height:38px;margin:12px 0;color:#a59eab;font-size:11px;line-height:1.5}.fd-signal-card footer{display:grid;grid-template-columns:repeat(3,1fr);gap:7px}.fd-signal-card footer span{padding:8px;border:1px solid rgba(255,255,255,.055);border-radius:9px;background:rgba(0,0,0,.18)}.fd-signal-card footer small{display:block;color:#5f5966;font-size:6px;letter-spacing:.1em}.fd-signal-card footer b{font-size:9px}.fd-alert-blur{filter:blur(5px);user-select:none}.fd-alert-lock{position:absolute;right:18px;top:57px;color:#ad77ff}.fd-alerts-empty{text-align:center;padding:70px 20px}.fd-alerts-empty>span{font-size:34px;color:#8e63c7}.fd-alerts-empty p{color:#817a88}@media(max-width:900px){.fd-signal-grid{grid-template-columns:1fr}.fd-alert-orbit{opacity:.25}.fd-alert-hero>div:first-child{max-width:90%}}@media(max-width:650px){.fd-alert-hero{padding:25px;min-height:360px}.fd-alert-counts{left:25px;gap:14px;flex-wrap:wrap}.fd-alerts-gate{display:block}.fd-signal-card footer{grid-template-columns:1fr 1fr}}`}</style></DashboardPageShell>}
